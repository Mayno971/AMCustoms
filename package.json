{
  "name": "am-customs-workspace",
  "version": "1.0.0",
  "description": "Environnement de développement AM Customs",
  "scripts": {
    "install:all": "npm install --prefix back && npm install --prefix front",
    "dev:front": "npm run dev --prefix front",
    "dev:back": "npx nodemon back/server.js",
    "dev": "npx concurrently -c \"blue,green\" -n \"BACK,FRONT\" \"npm run dev:back\" \"npm run dev:front\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0"
  }
}